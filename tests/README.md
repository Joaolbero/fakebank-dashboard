# Tests (FakeBank Dashboard)

Currently there are no automated tests configured.

This folder exists to document and (in the future) implement:
- UI tests
- Basic integration tests for JavaScript logic